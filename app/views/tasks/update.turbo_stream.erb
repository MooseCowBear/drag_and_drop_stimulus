<%= turbo_stream.replace "#{dom_id(@task)}_container" do %>
  <% if request.referrer == tasks_url || request.referrer == root_url %>
    <%= render "task", task: @task %>
  <% else %>
    <%= render "task", { task: @task, show: true } %>
  <% end %>
<% end %>